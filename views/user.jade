extends layout

block content
  newGameURL = '/user/' + account._id + '/game/new'
  .navMenu
    a(href=newGameURL)
      button.rightNavButton New Game
    a(href="/")
      button.leftNavButton Cancel
  .multilist
    ul.listgroup

  script.
    $(document).ready( function() {
        $('#expendituresMenu').attr('disabled',true);
        $('#incomeMenu').attr('disabled',true);
        $('#researchMenu').attr('disabled',true);
        $('#memesMenu').attr('disabled',true);
        $('#historyMenu').attr('disabled',true);

        // add each saved game to multilist here
    });
    